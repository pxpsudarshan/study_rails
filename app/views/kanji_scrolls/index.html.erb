<%= stylesheet_link_tag "jlpt", media: "all", 'data-turbo-track': 'reload' %>
<%= form_for :kanji, url: kanji_scrolls_path, html: { id: 'kanjis', role: 'form', method: :get }, remote: true do |f| %>
<% verbs = @gois.map{ |goi| [goi[:kanji_code], goi[:kanji_id]] } %>
<%= f.select :kanji_id, options_for_select(verbs,''), { include_blank: false }, class: 'form-select' %>
<%= f.submit 'next', class: 'btn btn-primary' %>
<% end %>
<div class="row">
  <div class="col-md-12" id="part"></div>
</div>
<div class="row">
  <div class="col-md-12" id="kanji_unit"></div>
</div>
