<%= stylesheet_link_tag "jlpt", media: "all", 'data-turbo-track': 'reload' %>
<%= form_for :kanji, url: kanjis_path, html: { id: 'kanjis', role: 'form', method: :get }, remote: true do |f| %>
<%= f.text_field :kanji, value: @goi, class: 'form-control' %>
<%= f.submit 'next', class: 'btn btn-primary' %>
<% end %>
<div class="row">
  <div class="col-md-12" id="part">
    <%= render "page_kanji_to_vocab" if @goi.present? %>
  </div>
</div>
<div class="row">
  <div class="col-md-12" id="kanji_unit"></div>
</div>
