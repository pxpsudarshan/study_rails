<div class="card w-50 m-auto border-3 rounded-0 border-student">
<%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post, class: 'form-horizontal text-center margin-auto' }) do |f| %>
  <%= embedded_svg 'niho_logo_newBlack.svg', class: 'm-4' %>
  <%= render "users/shared/error_messages", resource: resource %>
  <h1 class="h3 mb-3 fw-normal text-center"><%= t('.resend_unlock_instructions') %></h1>
  <div class="form-floating text-center">
    <%= f.email_field :email, autofocus: true, class: 'form-control', required: false %>
    <%= f.label :email %>
  </div>
  <%= f.submit t('.resend_unlock_instructions'), class: 'mt-3  m-auto btn btn-lg btn-dark rounded-0 ' %>
<% end %>
<p class="mt-5 m-4 text-center"><%= render "users/shared/links" %></p>
</div>
